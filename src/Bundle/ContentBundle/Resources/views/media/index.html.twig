{% extends '@IntegratedContent/base.html.twig' %}
{% block title %} {% trans %}Media Library{% endtrans %}{% endblock title %}
{% block content %}

    <div class="flex flex-wrap media-library">

        <div class="media-sidebar">
            <aside>
                <h2 class="heading ">{% trans %}Folders{% endtrans %}</h2>
                <div class="aside-holder bg-white section-radius">
                    <div class="aside-item-wrapper">
                        <div class="aside-folder-header">
                            <a href="{{ path(app.request.attributes.get('_route') ) }}">
                                <i class="iconoir-folder"></i><span
                                    class="p-4 text-black">{% trans %}All files{% endtrans %}</span>
                            </a>
                        </div>
                        <div class="aside-item-list-container">
                            <div class="aside-item-search">
                                <i class="iconoir-search"></i>
                                <input type="text" class="list-search"
                                       onchange="asideFolderSearch(this)"
                                       onkeyup="asideFolderSearch(this)"
                                       placeholder="{% trans %}Enter the name of a folder{% endtrans %}">
                            </div>

                            <form id="media_form" action="{{ path('integrated_content_media_manage_relations') }}">
                                <input type="hidden" id="media_category_csrf" name="token"
                                       value="{{ csrf_token('delete-item') }}"/>
                                <input type="hidden" id="media_id" name="media_id" value=""/>
                                <input type="hidden" id="category_id" name="category_id" value=""/>
                                <input type="hidden" id="channel_id" name="channel_id" value=""/>
                            </form>
                            <ul class="aside-filter-menu">
                                {% for key, menu_item in menu %}
                                    <li class="menu-parent {% if selectedMediaTaxonomy.current == menu_item.title %}current-menu-item{% endif %}">
                                        {% if selectedMediaTaxonomy.current == menu_item.title %}
                                            <a tabindex="-1" role="menuitem" class="media_category" id="{{ menu_item.ID }}">
                                        {% else %}
                                    <li class="menu-parent {% if params.media_taxonomy.current == menu_item.title %}current-menu-item{% endif %}">
                                        {% if params.media_taxonomy.current == menu_item.title %}
                                        <a tabindex="-1" role="menuitem" class="media_category" id="{{ menu_item.ID }}">
                                            {% else %}
                                            <a href="{{ path(app.request.attributes.get('_route'),
                                                app.request.query.all|
                                                merge({'MediaTaxonomy': menu_item.title == selectedMediaTaxonomy.default ? null : menu_item.title })|
                                                merge({'media_taxonomy_id':menu_item.ID})|
                                                merge({'page':1})
                                            ) }}"
                                               tabindex="-1"
                                               role="menuitem"
                                               class="media_category"
                                               id="{{ menu_item.ID }}">
                                                {% endif %}
                                                <i class="iconoir-folder"></i>
                                                <span>{{ menu_item.title|trans|capitalize }}</span>
                                            </a>
                                            {% if menu_item.children %}
                                                <ul class="child-menu">
                                                    {% for key_1, menu_item_1 in menu_item.children %}
                                                        <li class="menu-child {% if params.media_taxonomy.current == menu_item_1.title %}current-menu-item{% endif %}">
                                                            {% if params.media_taxonomy.current == menu_item_1.title %}
                                                            <a tabindex="-1" role="menuitem" class="media_category"
                                                               id="{{ menu_item_1.ID }}">
                                                                {% else %}
                                                                <a href="{{ path(app.request.attributes.get('_route'),
                                                                    app.request.query.all|
                                                                    merge({'MediaTaxonomy': menu_item_1.title == params.media_taxonomy.default ? null : menu_item_1.title })|
                                                                    merge({'media_taxonomy_id':menu_item_1.ID})|
                                                                    merge({'page':1})
                                                                ) }}"
                                                                   tabindex="-1"
                                                                   role="menuitem"
                                                                   class="media_category"
                                                                   id="{{ menu_item_1.ID }}">
                                                                    {% endif %}
                                                                    <i class="iconoir-folder"></i>
                                                                    <span>{{ menu_item_1.title|trans|capitalize }}</span></a>
                                                                {% if menu_item_1.children %}
                                                                    <ul class="child-menu">
                                                                        {% for key_2, menu_item_2 in menu_item_1.children %}
                                                                            <li class="menu-child {% if params.media_taxonomy.current == menu_item_2.title %}current-menu-item{% endif %}">
                                                                                {% if params.media_taxonomy.current == menu_item_2.title %}
                                                                                <a tabindex="-1" role="menuitem"
                                                                                   class="media_category"
                                                                                   id="{{ menu_item_2.ID }}">
                                                                                    {% else %}
                                                                                    <a href="{{ path(app.request.attributes.get('_route'),
                                                                                        app.request.query.all|
                                                                                        merge({'MediaTaxonomy': menu_item_2.title == params.media_taxonomy.default ? null : menu_item_2.title }) |
                                                                                        merge({'media_taxonomy_id':menu_item_2.ID})|
                                                                                        merge({'page':1})
                                                                                    ) }}"
                                                                                       tabindex="-1"
                                                                                       role="menuitem"
                                                                                       class="media_category"
                                                                                       id="{{ menu_item_2.ID }}">
                                                                                        {% endif %}
                                                                                        <i class="iconoir-folder"></i>
                                                                                        <span>{{ menu_item_2.title|trans|capitalize }}</span>
                                                                                    </a>
                                                                            </li>
                                                                        {% endfor %}
                                                                    </ul>
                                                                {% endif %}
                                                        </li>
                                                    {% endfor %}
                                                </ul>
                                            {% endif %}
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
            </aside>
        </div>
        <div class="media-gallery">
            <div class="page-title">
                <h1 class="heading">{% trans %}Media Library{% endtrans %}</h1>
                <ul class="options-no-bg">
                    <li class="dropdown">
                        <a href="#" data-toggle="dropdown" role="button" id="drop_1" class="btn btn-select">
                            <span>{% trans %}New{% endtrans %}</span>
                            <i class="iconoir-plus"></i>
                        </a>
                        <ul aria-labelledby="drop_1" role="menu" class="dropdown-menu" id="menu_1">
                            {% for type_key, type in contentTypeSelectOptions %}
                                {% if type %}
                                    <li role="presentation">
                                        <a href="{{ path('integrated_content_content_new') }}?type={{ type.getId }}">
                                            {{ type.getName | title }}
                                        </a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </li>
                </ul>
                <div class="options flex justify-between">
                    <div class="left flex">
                        <button class="toggle_view grid active switcher mr-2"><i class="iconoir-view-grid"></i></button>
                        <button class="toggle_view list switcher mr-2"><i class="iconoir-list"></i></button>
                        <ul class="mr-2">
                            <li class="dropdown">
                                <a href data-toggle="dropdown" role="button" id="sorting-list"
                                   class="btn btn-select space-between"><span>{{ params.content_types.options[params.content_types.current].label_plural|trans }}</span>
                                    <i class="iconoir-nav-arrow-down"></i></a>
                                <ul aria-labelledby="sorting-list" role="menu" class="dropdown-menu" id="sorting-menu">

                                    {% for content_type_key, content_type in contentTypeFilterOptions.options %}
{#                                        {{ dump(content_type) }}#}

                                        <li {% if content_type_key == contentTypeFilterOptions.current %}class="active"
                                            {% endif %}role="presentation">
                                            <a href="{{ path(app.request.attributes.get('_route'), app.request.query.all|
                                                merge({'class_string': content_type_key == contentTypeFilterOptions.default ? null : content_type_key })|
                                                merge({'page':1})
                                            ) }}"
                                               tabindex="-1"
                                               role="menuitem">{{ content_type.name|trans|capitalize }}</a>
                                        </li>

                                    {% endfor %}

                                </ul>
                            </li>
                        </ul>
                        <ul class="mr-2">
                            <li class="dropdown">
                                <a href data-toggle="dropdown" role="button" id="sorting-list"
                                   class="btn btn-select space-between"><span>{{ params.date_filter.options[params.date_filter.current].label|trans }}</span>
                                    <i class="iconoir-nav-arrow-down"></i></a>
                                <ul aria-labelledby="sorting-list" role="menu" class="dropdown-menu" id="sorting-menu">

                                    {% for year_month_key, year_month in dateFilterOptions.options %}
                                        {% if year_month.label != 'File' %}
                                            <li {% if year_month.name == dateFilterOptions.current %}class="active"
                                                {% endif %}role="presentation">
                                                <a href="{{ path(app.request.attributes.get('_route'), app.request.query.all|
                                                    merge({'year_month': year_month_key == dateFilterOptions.default ? null : year_month_key })|
                                                    merge({'page':1})
                                                ) }}"
                                                   tabindex="-1"
                                                   role="menuitem">{{ year_month.label|trans|capitalize }}</a>
                                            </li>
                                        {% endif %}
                                    {% endfor %}

                                </ul>
                            </li>
                        </ul>
                        <button class="btn btn-action mr-2"
                                id="bulkselection">{% trans %} Bulkselection {% endtrans %}
                        </button>

                        <button onclick="fetch_component_upload_images(event)" class="btn btn-action">
                            <i class="iconoir-add-media-image"></i>
                        </button>

                    </div>
                    <form action="" class="media-search-form" method="get">
                        <div class="form-group relative" style="width: 250px">
                            <input type="text" name="q" class="form-control"
                                   value="{{ app.request.query.get('q')|trim }}"
                                   placeholder="{% trans %}Zoeken{% endtrans %}...">
                            <button type="submit"><i class="iconoir-search"></i></button>
                        </div>
                    </form>
                </div>
            </div>
            <section class="section-white section-radius p-4">
                {# When you work on bulk select, use the class "mode-select" on media container when you activate bulk select #}
                <div class="media-container">
                    <div id="photo_upload_area">
                    </div>

                    <button id="modal_button"
                            class="block text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
                            type="button" data-modal-toggle="popup-modal">
                        Toggle modal
                    </button>

                    <div id="popup-modal" tabindex="-1"
                         class="hidden bg-red-200 overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 p-24 md:inset-0 h-modal md:h-full">
                        <div class="relative w-full max-w-md h-full md:h-auto">
                            <div class="relative bg-white rounded-lg shadow dark:bg-gray-700">
                                <button type="button"
                                        class="absolute top-3 right-2.5 text-gray-400 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm p-1.5 ml-auto inline-flex items-center dark:hover:bg-gray-800 dark:hover:text-white"
                                        data-modal-toggle="popup-modal">
                                    <svg aria-hidden="true" class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"
                                         xmlns="http://www.w3.org/2000/svg">
                                        <path fill-rule="evenodd"
                                              d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z"
                                              clip-rule="evenodd"></path>
                                    </svg>
                                    <span class="sr-only">Close modal</span>
                                </button>
                                <div id="popup-modal-content" class="p-6 text-center">
                                    <div>THIS CONTENT WILL BE REPLACED WITH FETCH RESULT</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="media-wrapper grid_view" style="">
                        {% if paginator.items|length == 0 %}
                            <div>
                                {% trans %} No results with this selection. {% endtrans %}
                            </div>
                        {% endif %}
                        <ul class="flex flex-wrap gallery ui-helper-reset ui-helper-clearfix" id="gallery">
                            {% for media in paginator.items %}

                                {% if media.contentType == 'image' %}
                                    {% set thumbnail = integrated_image(media.file).zoomCrop(200, 200) %}
                                    {#                                    {% set thumbnail = integrated_image(media.file) %} #}
                                {% elseif media.contentType == 'video' %}
                                    {% set thumbnail = 'data:image/png;base64,' %}
                                {% elseif media.contentType == 'file' %}
                                    {% set thumbnail = 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQS4vyXJNiQlY9mOgPqiZJ3Plom1W_vaRavbg&usqp=CAU' %}
                                {% else %}
                                    {% set thumbnail = 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQS4vyXJNiQlY9mOgPqiZJ3Plom1W_vaRavbg&usqp=CAU' %}
                                    {# TODO How to handle custom ContentTypes that can be anything? #}
                                    {# We could check pathname / identifier, but that is not so elegant# #}
                                {% endif %}

                                <li class="media-item ui-widget-content ui-corner-tr grid {{ media.contentType }}"
                                    id="{{ media.id }}" data-id="{{ media.id }}" data-media_id="{{ loop.index }}">
                                    <a href="{{ path('integrated_content_content_edit', {id: media.id}) }}">
                                        <div class="media-preview">
                                            <div class="thumbnail">
                                                <div class="centered">
                                                    <img src="{{ thumbnail }}"
                                                         alt="{{ media.title }}">
                                                </div>
                                            </div>
                                            {% if media.file | split('.')|last | lower not in not_shown_filetypes %}
                                                <div class="filetype">
                                                    {{ media.file|split('.')|last | lower }}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </a>
                                    <div class="details flex">
                                        <div class="w-full md:w-1/2">
                                            <div class="detail-wrap title">
                                                <span class="detail-label">{% trans %}Title{% endtrans %}:</span>
                                                <h3>{{ media.title }}</h3>
                                            </div>
                                            {% if media.description %}
                                                <div class="detail-wrap description">
                                                        <span
                                                            class="detail-label">{% trans %}Description{% endtrans %}:</span>
                                                    <p>{{ media.description }}</p>
                                                </div>
                                            {% endif %}
                                            {% if media.credits %}
                                                <div class="detail-wrap credits">
                                                        <span
                                                            class="detail-label">{% trans %}Credits{% endtrans %}:</span>
                                                    <p>{{ media.credits }}</p>
                                                </div>
                                            {% endif %}
                                        </div>
                                        <div class="w-full md:w-1/2">
                                            {% if media.createdAt is defined %}
                                                <div class="detail-wrap created-at flex justify-end">
                                                        <span
                                                            class="detail-label">{% trans %}Publish date{% endtrans %}:</span>
                                                    <p>{{ media.createdAt|format_date('long') }}</p>
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>

                                    <button id="bulkselection_{{ media.id }}" type="button"
                                            class="bulkselectionbutton check" tabindex="-1">
                                            <span class="media-modal-icon">
                                                <i class="iconoir-check"></i>
                                            </span>
                                        <span class="screen-reader-text">{% trans %} Deselect {% endtrans %}</span>
                                    </button>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>

                <div>
                    {% if paginator.getCustomParameter('amountOfPages') > 1 %}
                        {% for i in range(1, paginator.getCustomParameter('amountOfPages')) %}
                            {% if i == paginator.currentPageNumber %}
                                <a class="text-red-500 bg-red-200 p-2"
                                   href="{{ path(app.request.attributes.get('_route'), app.request.query.all|merge({'page': i })) }}"
                                   tabindex="-1" role="menuitem">{{ i }}</a>
                            {% else %}
                                <a class="p-2"
                                   href="{{ path(app.request.attributes.get('_route'), app.request.query.all|merge({'page': i })) }}"
                                   tabindex="-1" role="menuitem">{{ i }}</a>
                            {% endif %}
                        {% endfor %}

                        <span>{% trans %} Total {% endtrans %} {{ paginator.getTotalItemCount() }} {% trans %} Items {% endtrans %}.</span>
                        <span>{% trans %} Showing {% endtrans %} {{ paginator.getCustomParameter('showingStart') }} {% trans %} till {% endtrans %} {{ paginator.getCustomParameter('showingEnd') }}.</span>
                    {% endif %}
                </div>

            </section>
        </div>
    </div>

    <script>
        let toggle_view = 'GRID_VIEW';
        let show_modal = false;

        window.onload = function() {
            $('.toggle_view').on('click', function() {
                console.log('toggle');
                if (toggle_view == 'GRID_VIEW') {
                    $('.toggle_view.list').addClass('active');
                    $('.toggle_view.grid').removeClass('active');
                    $('.media-item').removeClass('grid');
                    $('.media-item').addClass('list');
                    toggle_view = 'TABLE_VIEW';
                } else {
                    $('.toggle_view.grid').addClass('active');
                    $('.toggle_view.list').removeClass('active');
                    $('.media-item').removeClass('list');
                    $('.media-item').addClass('grid');
                    toggle_view = 'GRID_VIEW';
                }
            });

            $('#modal_button').on('click', async function() {
                if (show_modal === false) {
                    $('#popup-modal').removeClass('hidden');
                    show_modal = true;

                    options = [];
                    const response = await fetch(
                        '/admin/content/e8e681dd4c693d5c8ba99ef5fc37c46a?lock=7f64bbff-7a90-4e2a-b7cc-21ad63794cec',
                        options);

                    const whereToAdd = '#popup-modal-content';
                    document.querySelector(whereToAdd).innerHTML = await response.text();
                } else {
                    $('#popup-modal').addClass('hidden');
                    show_modal = false;
                }
            });
        };

        let current_drop_target = '';

        async function fetch_component_upload_images(event) {
            const whereToAdd = '#photo_upload_area';
            const url = '/admin/media/upload';
            const options = [];

            //toggle if needed
            if (current_drop_target === whereToAdd) {
                $(`#photo_upload_area`).hide();
                current_drop_target = '';
                $('.media-wrapper').show();
                return;
            } else {
                $('.media-wrapper').hide();
                $(`#photo_upload_area`).show();
                current_drop_target = whereToAdd;
            }

            const response = await fetch(url, options);

            document.querySelector(whereToAdd).innerHTML = await response.text();

            let uppy = inititalizeUppy('/admin/media/upload_file');
        }

        function inititalizeUppy(endpoint_url) {
            let uppy = new Uppy({
                onBeforeFileAdded: (currentFile, files) => currentFile,
                onBeforeUpload(files) {

                    let current_category_id = new URL(location.href).searchParams.get('media_taxonomy_id') || '';

                    for (const [key, file] of Object.entries(files)) {
                        if (!file.meta.userCaption) {
                            file.meta.userTitle = 'Dit is mijn titel';
                            file.meta.category_id_target = current_category_id;
                        }
                    }
                },
            }).use(Dashboard, {
                inline: true,
                target: '#photo_upload_area',
                width: '100%',
                height: 400,
                proudlyDisplayPoweredByUppy: false,
                showProgressDetails: true,
                metaFields: [
                    {id: 'userTitle', name: 'Title', placeholder: 'file name'},
                    {id: 'userCaption', name: 'Description', placeholder: 'specify Description'},
                    {id: 'userTags', name: 'Tags', placeholder: 'Tags'},
                    // { id: 'category_id_target', name: 'Category', placeholder: 'specify Category' },
                ],
            });

            uppy.use(XHRUpload, {
                endpoint: endpoint_url,
            });

            uppy.use(ImageEditor, {
                target: Dashboard,
                quality: 0.8,
            });

            uppy.on('file-added', (file) => {
                uppy.setFileMeta(file.id, {
                    test: 'hello',
                });
            });

            uppy.on('complete', (result) => {
                console.log('Upload complete! We’ve uploaded these files:', result.successful);
            });

            return uppy;
        }

        let path = '{{ path('integrated_content_media_manage_relations') }}';
    </script>
{% endblock %}

{% block webpack_js %}
    <script src="{{ asset('bundles/integratedintegrated/mediagallery.js') }}"></script>
    <script src="{{ asset('bundles/integratedintegrated/app.js') }}"></script>
    <script>

        function asideFolderSearch(elem) {
            // Declare variables

            var input, filter, ul, li, a, i, txtValue;
            input = elem;
            filter = input.value.toUpperCase();
            ul = elem.parentNode.parentNode.querySelector('.aside-item-list-container > ul');
            li = ul.getElementsByTagName('li');

            // Loop through all list items, and hide those who don't match the search query
            for (i = 0; i < li.length; i++) {
                a = li[i].getElementsByTagName('a')[0];
                txtValue = a.textContent || a.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    li[i].classList.remove('menu-item-hidden');
                } else {
                    li[i].classList.add('menu-item-hidden');
                }
            }
        };
    </script>
{% endblock %}

{% block style %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('bundles/integratedintegrated/mediagallery.css') }}">
{% endblock %}
