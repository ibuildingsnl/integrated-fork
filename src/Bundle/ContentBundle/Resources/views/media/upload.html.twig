<div class="drag-drop-area" id="drag-drop-area"
     data-endpoint="{{ endpoint|default('/admin/media/upload_file') }}"
     data-language="{{ language|default('nl') }}"
     data-height="{{ height|default('400px') }}"
     data-imageEditorEnabled="{{ imageEditorEnabled|default(true) }}"
     data-target="{{ target|default('.drag-drop-areaa') }}">
</div>

<script src="{{ asset('bundles/integratedintegrated/upload_uppy.js') }}"></script>
