{% set bulkCategorySelectionHiddenContentHtml %}
    {{ form_widget(relation, {'attr': {
        'class': 'relation-items form-control',
        'data-value': dataValue,
        'data-taxonomy-popup': '1',
        'style': 'display:none',
    }}) }}
{% endset %}
{{ component('integrated_admin:taxonomy_category_picker', {
    rootId: 'taxonomy_category_' ~ relation.vars.id,
    rootClass: 'bulk-taxonomy-category',
    relationTitle: relation.vars.relation_title,
    categories: relation.vars.taxonomy_categories|default([]),
    checkboxIdPrefix: relation.vars.id ~ '_',
    allTabActive: true,
    hiddenContentHtml: bulkCategorySelectionHiddenContentHtml,
}) }}
