{% extends '@IntegratedContent/base.html.twig' %}
{% block title %}{% trans %}Search selections{% endtrans %}{% endblock title %}
{% block content %}
    <div class="row">
        <div class="w-full mb-4 md:mb-0">
            <section class="section-white section-radius">
                <table class="table table-hover">
                    <colgroup>
                        <col />
                        <col />
                        <col style="width: 5em" />
                    </colgroup>

                    <thead>
                        <tr>
                            <th>{% trans %}Search selections{% endtrans %}</th>
                            <th>{% trans %}Short code{% endtrans %}</th>
                            <th>{% trans %}Actions{% endtrans %}</th>
                        </tr>
                    </thead>

                    <tbody>
                        {% for searchSelection in searchSelections %}

                            <tr>
                                <td>{{ searchSelection.title }}</td>
                                <td>{{ searchSelection.id|default }}</td>
                                <td>
                                    {% if not searchSelection.locked %}
                                    <a href="{{ path('integrated_content_search_selection_edit', { 'id': searchSelection.id }) }}" title="{% trans %}Edit{% endtrans %}"><span class="fa fa-edit"></span></a>
                                    <a href="{{ path('integrated_content_search_selection_delete', { 'id': searchSelection.id }) }}" title="{% trans %}Delete{% endtrans %}"><span class="fa fa-xmark-circle"></span></a>
                                    {% endif %}
                                </td>
                            </tr>

                        {% endfor %}
                    </tbody>
                </table>

                {{ knp_pagination_render(searchSelections) }}
            </section>
        </div>
    </div>

{% endblock %}
