{% extends '@IntegratedContent/base.html.twig' %}
{% block title %}{% trans %}Channels{% endtrans %}{% endblock title %}
{% block content %}
    <div class="row">
        <div class="w-full mb-4 md:mb-0">
            <div class="page-title">
                <h1 class="heading">{% trans %}Channels{% endtrans %}</h1>
                <ul class="create">
                    <li>
                        <a class="btn btn-create-new" href="{{ path('integrated_content_channel_new') }}">
                            <span>{% trans %}New{% endtrans %} {% trans %}channel{% endtrans %}</span>
                            <i class="fa-regular fa-plus"></i>
                        </a>
                    </li>
                </ul>
            </div>

            <section class="section-white section-radius">

                <table class="table table-hover">
                    <thead>
                    <tr>
                        <th>{% trans %}Name{% endtrans %}</th>
                        <th>{% trans %}Id{% endtrans %}</th>
                        <th>{% trans %}Domains{% endtrans %}</th>
                        <th>{% trans %}Created{% endtrans %}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for document in documents %}
                        <tr class="has-options">
                            <td>
                                <strong>
                                    <a href="{{ path('integrated_content_channel_show', {'id': document.id}) }}">
                                        {{ document.name }}
                                    </a>
                                </strong>
                                <div class="row-options">
                                    <a href="{{ path('integrated_content_channel_edit', {'id': document.id}) }}" title="{% trans %}Edit{% endtrans %}">
                                        {% trans %}Edit{% endtrans %}
                                    </a> |
                                    <a class="color-red" href="{{ path('integrated_content_channel_delete', {'id': document.id}) }}" title="{% trans %}Delete{% endtrans %}">
                                        {% trans %}Delete{% endtrans %}
                                    </a>
                                </div>
                            </td>
                            <td>
                                {{ document.id }}
                            </td>
                            <td>
                                {{ document.domains|join(', ') }}
                            </td>
                            <td>
                                {{ document.createdAt|date }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4">
                                {% trans %}No channels added{% endtrans %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </section>
        </div>
    </div>
{% endblock %}
