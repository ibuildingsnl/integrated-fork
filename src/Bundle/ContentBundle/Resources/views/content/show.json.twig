{% apply spaceless %}
{% set data = {
    'id'   : document.id,
    'title': document.title|default(document.name|default),
} %}
{% endapply %}
{{ data|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
