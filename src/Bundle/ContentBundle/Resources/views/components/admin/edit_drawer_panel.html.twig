<div class="{{ this.holderClasses }}">
    <div class="aside-item-wrapper" id="{{ wrapperId }}" data-editImagePath="{{ editImagePath }}" data-editImageIframePath="{{ editImageIframePath }}">
        <button class="close-media-edit-form">
            <i class="iconoir-xmark"></i></button>
        <turbo-frame id="{{ frameId }}" data-media-id="{{ mediaId }}" data-file="{{ file }}"></turbo-frame>
    </div>
</div>
