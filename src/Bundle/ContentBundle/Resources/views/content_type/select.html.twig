{% extends '@IntegratedContent/base.html.twig' %}
{% block title %}{% trans %}Select content type{% endtrans %}{% endblock title %}

{% block content %}
    <div class="row">
        <div class="index-wrapper-full">
            <div class="page-title">
                <h1 class="heading">{% trans %}Select document type{% endtrans %}</h1>
            </div>
            <section class="section-white section-padding section-radius">
                <form method="get" action="{{ path('integrated_content_content_type_new') }}">
                    <div class="form-group">
                        <label>Document type</label>
                        <select name="class" class="form-control">
                            {% for type in documentTypes %}
                                {% if type.type %}
                                    <option value="{{ type.class }}">{{ type.type }}</option>
                                {% endif %}
                            {% endfor %}
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">
                        {% trans %}Submit{% endtrans %}
                    </button>
                </form>
            </section>
        </div>
    </div>
{% endblock %}
