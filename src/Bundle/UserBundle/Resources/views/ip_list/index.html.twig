{% extends '@IntegratedContent/base.html.twig' %}
{% block title %}{% trans %}IP list{% endtrans %} {% endblock title %}
{% block content %}
    <div class="row">
        <div class="index-wrapper-full">
            {% set createActionsHtml %}
                <ul class="create">
                    <li>
                        <a class="btn btn-create-new" href="{{ path('integrated_user_iplist_new') }}">
                            <span>{% trans %}New{% endtrans %} {% trans %}IP list{% endtrans %}</span>
                            <i class="iconoir-plus"></i>
                        </a>
                    </li>
                </ul>
            {% endset %}
            {{ component('integrated_admin:page_title', {
                title: 'IP list'|trans,
                actionsHtml: createActionsHtml,
            }) }}
            {% component 'integrated_admin:section_card' %}
                {% block content %}
                {% set ipListTableColGroupHtml %}
                    <colgroup>
                        <col style="width: 3em" />
                        <col style="width: 20em" />
                        <col />
                        <col style="width: 5em" />
                    </colgroup>
                {% endset %}
                {% set ipListTableHeadHtml %}
                    <tr>
                        <th>#</th>
                        <th>{% trans %}Ip{% endtrans %}</th>
                        <th>{% trans %}Description{% endtrans %}</th>
                    </tr>
                {% endset %}
                {% set ipListTableBodyHtml %}
                    {% for list in lists %}
                        <tr class="has-options">
                            <td>{{ list.id }}</td>
                            <td>
                                <strong>
                                    <a href="{{ path('integrated_user_iplist_edit', {'id': list.id}) }}">
                                        {{ list.ip.protocolAppropriateAddress }}
                                    </a>
                                </strong>
                                {% set ipListRowActionsHtml %}
                                    <a href="{{ path('integrated_user_iplist_edit', {'id': list.id}) }}"
                                       title="{% trans %}Edit{% endtrans %}">
                                        {% trans %}Edit{% endtrans %}
                                    </a> |
                                    <a class="color-red"
                                       href="{{ path('integrated_user_iplist_delete', {'id': list.id}) }}"
                                       title="{% trans %}Remove{% endtrans %}">
                                        {% trans %}Remove{% endtrans %}
                                    </a>
                                {% endset %}
                                {{ component('integrated_admin:row_actions', {
                                    contentHtml: ipListRowActionsHtml,
                                }) }}
                            </td>
                            <td>{{ list.description }}</td>
                        </tr>
                   {% endfor %}
                {% endset %}
                {{ component('integrated_admin:data_table', {
                    colGroupHtml: ipListTableColGroupHtml,
                    headHtml: ipListTableHeadHtml,
                    bodyHtml: ipListTableBodyHtml,
                    colSpan: 3,
                }) }}

                {{ component('integrated_admin:pagination_footer', {
                    pagination: lists,
                }) }}
                {% endblock %}
            {% endcomponent %}
        </div>
    </div>
{% endblock %}
