{% extends "@IntegratedUser/layout.html.twig" %}

{% block style %}
    {{ parent() }}

    {% include('@IntegratedUser/partial/modal.html.twig') %}
{% endblock style %}

{% block flash %}
{% endblock flash %}

{% block body %}
    <div class="modal-wrapper auth">
        <div class="logo"><img src="{{ asset('bundles/integratedintegrated/images/logo_white.svg') }}" alt=""></div>
        <div class="auth modal">
            <div class="modal-dialog">
                <div class="modal-content ">

                    {{ form_start(form, {'attr': {'id': 'login_form'}, 'widget_col': 'w-full', 'label_col': 'w-full', 'style': 'inline', 'show_label': 'false'}) }}
                    <div class="modal-header">
                        <h2 class="modal-title">{% trans %}Login{% endtrans %}</h2>
                        <p>{% trans %} Fill in your data to log into your account {% endtrans %}</p>
                    </div>

                    <div class="modal-body">
                        {% for label, messages in app.flashes(['alert', 'danger', 'info', 'success', 'warning', 'error']) %}
                            {% for message in messages %}
                                {% if label == 'alert' %}
                                    {% set alertVariant = 'warning' %}
                                {% elseif label == 'error' %}
                                    {% set alertVariant = 'danger' %}
                                {% else %}
                                    {% set alertVariant = label %}
                                {% endif %}
                                {{ component('integrated_admin:alert_box', {
                                    variant: alertVariant,
                                    dismissible: true,
                                    bodyHtml: message|trans,
                                }) }}
                            {% endfor %}
                        {% endfor %}

                        {% if form.vars.errors|length > 0 %}
                            {{ component('integrated_admin:alert_box', {
                                variant: 'info',
                                bodyHtml: 'Login help: use your username or e-mail address, and verify that login is enabled and your account has admin scope access.'|trans,
                            }) }}
                        {% endif %}

                        {{ form_widget(form) }}
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
        <span class="password-reset">
            <a href="{{ path('integrated_user_password_reset') }}">{% trans %}Password reset{% endtrans %}</a>
        </span>
    </div>

    <div class="modal-backdrop"></div>
{% endblock body %}

{% block javascript %}
    {{ parent() }}
    {% include('@IntegratedUser/partial/password_toggle_script.html.twig') %}
{% endblock javascript %}
