{% apply spaceless %}
{% set data = {
    'id'   : block.id,
    'title': block.title,
    'type' : block.type,
    'html' : integrated_block(block),
    'cssClass' : block.cssClass
} %}
{% endapply %}
{{ data|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
