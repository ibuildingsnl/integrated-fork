<!DOCTYPE html>
<html>
<head>
    <title>{{ newsletter.title }} {{ date().format('d/m/Y') }}</title>
</head>
<body>

<h1>
{{ newsletter.title }}
</h1>

{% for item in content.content %}
    <h3>
    {{ item.title }}
    </h3>
    <div>
    {{ item }}
    </div>
{% endfor %}

</body>
</html>
