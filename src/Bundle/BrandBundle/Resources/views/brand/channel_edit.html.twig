{% extends '@IntegratedContent/base.html.twig' %}

{% block title %}{% trans %}Edit channel{% endtrans %}{% endblock title %}

{% block style %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ asset('bundles/integratedintegrated/edit.css') }}">
{% endblock %}

{% block content %}
    <div class="flex flex-wrap edit-form">
        {% block form_start %}
            {{ form_start(form) }}
        {% endblock %}
        {% block toolbar %}
            {% include '@IntegratedContent/partials/block.toolbar.html.twig' %}
        {% endblock toolbar %}
        <div class="aside-options">
            <aside>
                <div class="aside-holder">
                    {% block sidebar %}
                        <div class="aside-header">
                            <button class="toggle-filters toggle-options-sidebar"  data-target="aside-options" type="button">
                                <i class="iconoir-xmark"></i></button>
                            <span>{% trans %}Options{% endtrans %}</span>
                        </div>
                        {% block content_meta %}
                            {% for child in form|filter(row => row.vars.attr.location|default == constant('Integrated\\Common\\Form\\Mapping\\Attributes\\Field::LOCATION_SIDEBAR'))  %}
                                {{ form_row(child) }}
                            {% endfor %}
                        {% endblock %}
                    {% endblock %}
                </div>
            </aside>
        </div>
        <div class="editor-wrapper">
            {% set channelEditActionsHtml %}
                {% if link|default and link.type.connector and link.type.connector in availableConnectors|default([]) %}
                    <ul class="create">
                        <li>
                            <a class="btn btn-create-new" href="{{ path('integrated_content_brand_manage_connector', {'id': brand.id, 'link': link.id}) }}" title="{% trans %}Connector{% endtrans %}">
                                {% trans %}Manage{% endtrans %} {% trans %}Connector{% endtrans %}
                            </a>
                        </li>
                    </ul>
                {% endif %}
            {% endset %}
            {{ component('integrated_admin:page_title', {
                title: ('Edit'|trans) ~ ' ' ~ channel.name,
                actionsHtml: channelEditActionsHtml|trim,
            }) }}
            <section class="editor editor-wrapped">
                {{ form_errors(form) }}

                {% block content_form %}
                    {% for child in form|filter(row => row.vars.attr.location|default == constant('Integrated\\Common\\Form\\Mapping\\Attributes\\Field::LOCATION_EDITOR'))  %}
                        {{ form_row(child) }}
                    {% endfor %}

                    {{ form_rest(form, {'style':'editor', 'state':'show'})}}
                {% endblock %}
            </section>
        </div>

        {% block form_end %}
            {{ form_end(form) }}
        {% endblock %}
    </div>
{% endblock %}

{% block webpack_js %}
    <script src="{{ asset('bundles/integratedintegrated/edit.js') }}" defer data-turbo-track="reload"></script>
{% endblock %}

{% block javascript %}
{{ parent() }}

<script src="{{ asset('bundles/integratedcontent/js/channel.js') }}" defer data-turbo-track="reload"></script>
{% endblock javascript %}
